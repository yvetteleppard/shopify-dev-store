{% if title != blank %}
  <header class="page-title">
    <h1 class="page-title__title">{{ title }}</h1>
    {% if subtitle != blank %}
      <h5 class="page-title__subtitle">{{ subtitle }}</h5>
    {% endif %}
  </header>
{% endif %}
